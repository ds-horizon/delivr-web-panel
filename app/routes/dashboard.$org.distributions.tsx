/**
 * Distributions - Parent Layout Route
 * Wraps all distribution-related pages
 */

import { Outlet } from '@remix-run/react';

export default function DistributionsLayout() {
  return <Outlet />;
}

